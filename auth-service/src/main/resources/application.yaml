server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/devmatch_auth
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

logging:
  level:
    org.flywaydb: DEBUG
    org.springframework.boot.autoconfigure.flyway: DEBUG

#  security:
#    oauth2:
#      client:
#        registration:
#          google:
#            client-id:
#            client-secret:
#            scope: profile, email
#            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationID}"
#            client-name: Google

jwt:
  secret: /twA7/Xxl4oC3WNBty3wNL94d/+4/mehRhkatN5lblI=
  access-token-expiration: 900000 #15 min
  refresh-token-expiration: 604800000 #7 days

user-service:
  url: http://localhost:8082
